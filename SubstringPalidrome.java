public class SubstringPalidrome {
        static String  subString(String str){
                if(str.length() == 0){
                        return  str;
                }
                String smallString = str.substring(1);
                return smallString;
        }
        public static void main(String[] args) {
               String str = "abbabcca";
                int left = 0;
                 int right = str.length()-1;
                 while(left <= right){
                    
                 }
        }
}
